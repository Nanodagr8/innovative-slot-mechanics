apiVersion: v1
kind: Service
metadata:
  name: uranus-spins
spec:
  type: { { .Values.service.type } }
  selector:
    app: uranus-spins
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.port } }
